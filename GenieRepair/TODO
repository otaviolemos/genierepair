TODO LIST:


remove slice by checking the methods source code, and not restoring old files

in the failing methods pool, don't clean every thing every time a test case session starts,
	 just change icons to differ from failing and successful test case
	 
 
